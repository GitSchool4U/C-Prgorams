// program to print ascii value of any character entered by user.

#include <stdio.h>

int main()
{
    int num;

    // input character
    printf("Enter the number: ");
    scanf("%d", &num);

    printf("ASCII is = %c\n", num);

    return 0;
}